FROM node:20-slim

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy the rest of the code is handled by volumes in compose
# for hot-reloading, but we expose the Vite port
EXPOSE 3000

# Run Vite in dev mode with network access
CMD ["npm", "run", "dev", "--", "-H", "0.0.0.0"]